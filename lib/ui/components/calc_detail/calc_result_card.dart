import 'package:flutter/material.dart';

class CalcResultCard extends StatelessWidget {
  const CalcResultCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
